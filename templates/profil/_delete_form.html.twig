<form method="post" action="{{ path('app_profil_delete', {'PER_ID': profil.PER_ID}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil.PER_ID) }}">
    <button class="btn">Delete</button>
</form>
