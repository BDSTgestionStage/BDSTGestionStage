{% extends 'base.html.twig' %}

{% block title %}Modifier {{ entreprise.ENTNOM }}{% endblock %}

{% block body %}
{% include '/index.html.twig' %}
    <h1>Modifier {{ entreprise.ENTNOM }}</h1>

    {{ include('entreprise/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="tripbtn">

    <button class="btn btns"><a class="return" href="{{ path('app_entreprise_index') }}"><i class="fa-solid fa-house"></i></a>
</i></a>

    </div>
    <div class="sousbtn">
    {{ include('entreprise/_delete_form.html.twig') }}</div>
    
{% endblock %}
